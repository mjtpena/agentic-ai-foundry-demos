# =============================================================================
# Accelerate Agentic AI — Python dependencies for all code demos (Days 1-3)
# Install:  python -m venv .venv && source .venv/bin/activate
#           pip install -r requirements.txt
# (Day 1 #4 hosted agent has its own requirements.txt for the azd container.)
# =============================================================================

# Core auth + config — used by every demo
azure-identity>=1.19
python-dotenv>=1.0

# Day 1 #3 prompt agent · Day 2 #7 OpenAPI · Day 2 #8 A2A
# NOTE: A2ATool (demo 8) lives in the PRERELEASE build. After this file, run:
#   pip install --pre "azure-ai-projects[agents]"
azure-ai-projects>=2.0.0          # Foundry projects (new) API

# Day 2 #6 MCP tools agent — McpTool is PRERELEASE-only. The demo's install is:
#   pip install --pre azure-ai-projects azure-ai-agents mcp
azure-ai-agents>=1.2.0b1
mcp>=1.2.0

# Day 2 #7 OpenAPI tool
jsonref>=1.1.0

# Day 2 #12 · Day 1 #4 — Microsoft Agent Framework
agent-framework

# Day 2 #9/#10 agentic retrieval (preview objects: KnowledgeSource/KnowledgeBase)
azure-search-documents>=11.7.0b1

# Day 3 #16 guardrails / content safety
azure-ai-contentsafety>=1.0.0
