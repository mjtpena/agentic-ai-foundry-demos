# =============================================================================
# Accelerate Agentic AI — Python dependencies for all code demos (Days 1-3)
# Install:  python -m venv .venv && source .venv/bin/activate
#           pip install -r requirements.txt
# (Day 1 #4 hosted agent has its own requirements.txt for the azd container.)
# =============================================================================

# Core auth + config — used by every demo
azure-identity>=1.19
python-dotenv>=1.0

# Day 1 #3 prompt agent · Day 2 #7 OpenAPI · Day 2 #8 A2A
# NOTE: A2ATool (demo 8) lives in the PRERELEASE build. After this file, run:
#   pip install --pre "azure-ai-projects[agents]"
azure-ai-projects>=2.0.0          # Foundry projects (new) API

# Day 2 #6 MCP tools agent — McpTool is PRERELEASE-only. The demo's install is:
#   pip install --pre azure-ai-projects azure-ai-agents mcp
azure-ai-agents>=1.2.0b1
mcp>=1.2.0

# Day 2 #7 OpenAPI tool
jsonref>=1.1.0

# Day 2 #12 · Day 1 #4 — Microsoft Agent Framework
agent-framework

# Day 2 #9/#10 agentic retrieval (preview objects: KnowledgeSource/KnowledgeBase)
azure-search-documents>=11.7.0b1

# Day 3 #16 guardrails / content safety
azure-ai-contentsafety>=1.0.0

# Day 3 #19 observability — OpenTelemetry tracing of agent runs.
# (azure-monitor-opentelemetry is optional: only needed to export spans to
#  Application Insights / the Control Plane Observability pane.)
opentelemetry-sdk>=1.27
# azure-monitor-opentelemetry>=1.6        # optional cloud export

# azure-ai-inference powers the observability demo + the unified UI console.
# Published as beta only (no stable 1.0.0 yet), so pin to the beta floor:
azure-ai-inference>=1.0.0b9

# Day 3 #17 evaluations · #18 AI Red Teaming Agent — Foundry evaluation SDK.
# The live console runs always-on variants of these (no extra deps), so this is
# only needed to run the standalone scripts directly. It's heavy (RedTeam pulls
# in PyRIT), so install it on demand rather than as a base dependency:
#   pip install azure-ai-evaluation               # demo 17 evaluators
#   pip install "azure-ai-evaluation[redteam]"    # demo 18 AI Red Teaming Agent
