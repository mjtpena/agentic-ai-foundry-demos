# Foundry Live Demo Console — web app deps (in addition to ../requirements.txt).
# Install into the same venv:
#     pip install -r requirements.txt
#     pip install -r ui/requirements-ui.txt
#     pip install --pre "azure-ai-projects[agents]" azure-ai-agents   # preview tools (MCP/A2A)
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
jsonref>=1.1
