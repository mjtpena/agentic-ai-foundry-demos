"""Demo wrappers. Each module exposes run(stream, payload) and optionally
setup(stream, payload) / status(). SDK imports live inside functions so a
missing preview package degrades a single demo, not the whole server."""
