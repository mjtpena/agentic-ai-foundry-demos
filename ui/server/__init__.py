"""Foundry Live Demo Console — FastAPI backend.

Wraps the existing Day 1-3 console demos behind a small HTTP/SSE API so a
branded web UI can drive them and surface what happens on the Microsoft
Foundry side (agents, tool calls, run steps, retrieval plans, guardrails).
"""
